#include "main.h"
#include <stdio.h>

/**
 * handle_di - Handle the 'i' and 'd' conversion specifiers
 * @args: The vector argument list of arguments.
 *
 * Return: The number of characters printed.
 */
int handle_di(va_list args)
{
	int num = va_arg(args, int);
	int char_count = 0;

	return (char_count);
}
